# 📚 Library Management System API (Node.js & SQLite)

Backend API สำหรับระบบจัดการห้องสมุด  
พัฒนาด้วย **Node.js + SQLite**  
ออกแบบตามสถาปัตยกรรม **Layered Architecture (Route → Controller → Service → Database)**  
รองรับระบบ **ยืม–คืนหนังสือ พร้อมคำนวณค่าปรับอัตโนมัติ**

---

## 🛠 การติดตั้งและเริ่มใช้งาน (Installation)

### 1. ติดตั้ง Dependencies
```bash
npm install
```
2. เตรียมฐานข้อมูลและข้อมูลตัวอย่าง
```bash
npm run init-db

```

3. รันเซิร์ฟเวอร์

```bash
# สำหรับพัฒนา (ใช้ nodemon)
npm run dev

# สำหรับรันปกติ
node server.js




```

API จะทำงานที่:
```bash
http://localhost:3000
```


รายละเอียด API (API Documentation)
1. หมวดหนังสือ (Books)

| Method | Endpoint                  | Description                    |
| ------ | ------------------------- | ------------------------------ |
| GET    | `/api/books`              | ดึงข้อมูลหนังสือทั้งหมด        |
| GET    | `/api/books/:id`          | ดึงข้อมูลหนังสือตาม ID         |
| GET    | `/api/books/search?q=...` | ค้นหาหนังสือจากชื่อหรือผู้แต่ง |
| POST   | `/api/books`              | เพิ่มหนังสือใหม่               |
| PUT    | `/api/books/:id`          | แก้ไขข้อมูลหนังสือ             |



2. หมวดสมาชิก (Members)
| Method | Endpoint       | Description            |
| ------ | -------------- | ---------------------- |
| GET    | `/api/members` | ดึงข้อมูลสมาชิกทั้งหมด |
| POST   | `/api/members` | สมัครสมาชิกใหม่        |



3. ระบบยืม–คืนหนังสือ (Borrowing & Returning)
🟢 การยืมหนังสือ (Borrow)

Endpoint:
```bash
POST /api/borrowings/borrow
```


เงื่อนไขการยืม

หนังสือต้องมีเล่มว่าง (available_copies > 0)

สมาชิกต้องมีสถานะ active

สมาชิกยืมได้ไม่เกิน 3 เล่ม

ตัวอย่าง Request Body


```bash
{
  "book_id": 1,
  "member_id": 1
}
```

การคืนหนังสือ (Return)

Endpoint:

```bash
PUT /api/borrowings/:id/return

```


รายละเอียด

เปลี่ยนสถานะเป็น returned

คืนสต็อกหนังสือเข้าตาราง Books

คำนวณค่าปรับกรณีคืนเกินกำหนด
👉 ค่าปรับ วันละ 20 บาท

ตัวอย่าง Response (คืนช้า 3 วัน)


```bash
{
  "success": true,
  "message": "Book returned successfully",
  "data": {
    "id": 1,
    "return_date": "2026-01-10",
    "days_overdue": 3,
    "fine": 60
  }
}


```
